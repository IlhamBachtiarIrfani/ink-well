import { DataFormatInterceptor } from './data-format.interceptor';

describe('DataFormatInterceptor', () => {
  it('should be defined', () => {
    expect(new DataFormatInterceptor()).toBeDefined();
  });
});
