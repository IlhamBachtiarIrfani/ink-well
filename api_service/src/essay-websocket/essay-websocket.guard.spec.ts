import { EssayWebsocketGuard } from './essay-websocket.guard';

describe('EssayWebsocketGuard', () => {
  it('should be defined', () => {
    expect(new EssayWebsocketGuard()).toBeDefined();
  });
});
